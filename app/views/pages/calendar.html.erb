<%= month_calendar(events: @wastes ) do |date,wastes| %>

  <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-4 mb-4">
    <h2 class="text-lg font-semibold mb-2">
      <%= date.strftime("%d %B %Y") %>
    </h2>
    <%wastes.each do |waste|%>
      <table class="">
        <thead>
          <tr>
            <th>
              Matricule
            </th>
            <th>
              Date depot
            </th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>
              <%=waste.reg_number%>
            </td>
            <td>
              <%=waste.created_at.strftime("%d %B %Y")%>
            </td>
          </tr>
        </tbody>
      </th>
    </table>
  <%end%>
</div>
<% end %>
